name: Build C Windows Executable

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_windows:
    runs-on: codeberg-tiny
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # --- 1. Install Core MinGW Toolchain and Build Dependencies (CRITICAL FIX: Single Line Install) ---
      - name: Install MinGW Tools and Build Dependencies
        run: |
          # CRITICAL FIX: All packages on a single line to prevent shell line-continuation errors.
          sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends mingw-w64 gcc-mingw-w64-x86-64 build-essential autoconf libtool git
          
      # --- 2. Build Libcurl from Source for MinGW (Guaranteed Compatibility) ---
      - name: Build Static Libcurl for MinGW
        run: |
          # Define environment variables for cross-compilation
          export TARGET_HOST=x86_64-w64-mingw32
          export CFLAGS="-m64"
          
          # Clone libcurl source
          git clone https://github.com/curl/curl.git
          cd curl
          
          # Run autoconf to generate the configure script
          ./buildconf
          
          # Configure for static cross-compilation
          ./configure \
              --host=$TARGET_HOST \
              --build=x86_64-pc-linux-gnu \
              --disable-shared \
              --enable-static \
              --with-winssl \
              --without-ssl \
              --disable-ldap --disable-ldaps \
              --disable-ftp --disable-file --disable-tftp --disable-pop3 --disable-imap --disable-smtp --disable-rtsp \
              --disable-dict --disable-telnet --disable-samba --disable-mqtt \
              --prefix=$GITHUB_WORKSPACE/curl_build 

          # Compile and install
          make -j$(nproc)
          make install
          
          # Move the library and headers to the expected locations
          mkdir -p lib
          cp $GITHUB_WORKSPACE/curl_build/lib/libcurl.a $GITHUB_WORKSPACE/lib/
          
          # Headers are handled in the next step, but copy them too just in case
          mkdir -p mingw-include
          cp -r $GITHUB_WORKSPACE/curl_build/include/* $GITHUB_WORKSPACE/mingw-include/

      # --- 3. Manually Download and Setup Dependencies (Minimal Cleanup) ---
      - name: Download cJSON and cURL Headers Locally (for CJSON and dummy headers)
        run: |
          # 3a: Setup cJSON (Source files) - Fixes 'cjson/cJSON.h: No such file'
          mkdir -p cjson
          curl -L -o cjson/cJSON.c https://raw.githubusercontent.com/DaveGamble/cJSON/master/cJSON.c
          curl -L -o cjson/cJSON.h https://raw.githubusercontent.com/DaveGamble/cJSON/master/cJSON.h
          
          # 3b: Setup cURL Headers (Dummy files only, real headers are from the build step)
          mkdir -p mingw-include/curl
          # Create DUMMY files for all known generated/unreliable cURL headers.
          touch mingw-include/curl/system.h
          touch mingw-include/curl/header.h
          touch mingw-include/curl/websockets.h
          touch mingw-include/curl/mprintf.h
          touch mingw-include/curl/typecheck-gcc.h
          
      # --- 4. Compile the Windows Executable (FINAL COMMAND) ---
      - name: Compile aslookup_win.c for Windows
        run: |
          # Uses all necessary flags for MinGW cross-compilation
          # Link directly to the freshly built lib/libcurl.a file
          x86_64-w64-mingw32-gcc -o aslookup_win.exe aslookup_win.c cjson/cJSON.c \
              -D_WIN32_WINNT=0x0600 \
              -DCURL_STATICLIB \
              -DCURL_IS_STATIC \
              -Dcurl_off_t="long long" \
              -D_CRT_DECLARE_NONSTDC_NAMES \
              -I. \
              -I./mingw-include \
              ./lib/libcurl.a -lws2_32 -ldnsapi -static

      - name: Verify Windows Executable Size
        run: ls -l aslookup_win.exe
          
      - name: Upload Windows Executable
        uses: actions/upload-artifact@v4
        with:
          name: aslookup-windows
          path: aslookup_win.exe